import { StudentCourseModel } from './student-course-model';

describe('StudentCourseModel', () => {
  it('should create an instance', () => {
    expect(new StudentCourseModel()).toBeTruthy();
  });
});
